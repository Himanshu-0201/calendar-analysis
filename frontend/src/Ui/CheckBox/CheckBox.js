

const CheckBox = (isChecked) => {

    return (
        <input
            type="checkbox"
            className="cursor-pointer"
            // checked= "defaultChecked"
        />
    )
}

export default CheckBox;